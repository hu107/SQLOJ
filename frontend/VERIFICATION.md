# 验证记录

## 2026-09-23 示例表名并入题目描述（当前版本）

- 取消尚未落库的 `table_structure` 方案，Java 实体、DTO、管理员详情、用户详情、MyBatis 与前端请求响应均不再包含 `tableStructure`。
- 管理端只填写一个题目描述，格式为“查询要求 + 空行 + `示例：表名`”；详情按原换行展示，不再单独显示表结构区块。
- 真实数据库 50 / 50 道题已更新为新格式，例如 `示例：employees`；多表题目使用顿号分隔。`business_user` 没有 `ALTER` 权限，因此真实库中的 `table_structure` 物理列仍需管理员执行 `database/migrations/20260923_remove_question_table_structure.sql` 的最后一条删列语句，应用代码已经不再读取或写入该列。该列当前为 `NOT NULL` 且无默认值，完成删列前不要通过新代码新增题目。
- `npm run build`：通过；Element Plus 主包仍有原有的 500 kB 体积提醒。
- `$env:PLAYWRIGHT_CHANNEL='chrome'; npm run test:e2e`：**23 / 23 通过**，覆盖新增、编辑、详情及描述换行原样传递。
- JDK 21 下根目录 `mvn test` 与 `mvn package -DskipTests`：通过，已生成后端可运行 JAR；当前工程没有 Java 自动化测试用例。

## 2026-09-20 编辑删除恢复到列表（当前版本）

- 列表右侧恢复固定的“操作”列，直接提供编辑、删除；点击标题仍打开只读详情，详情底部仅保留关闭按钮。取消编辑/删除返回列表，删除仍需二次确认。标题悬停无下划线。
- `npm run build`：通过，用时 17.19 秒，保留已有 Element Plus 包体积提醒。
- 运行 `tests/questions-crud.spec.js`：**11 / 11 通过**，用时 23.8 秒。验证直接编辑、删除确认与取消、失败保留数据、分页回退、标题打开详情、详情重试和手机交互，未重复运行未修改的登录注册用例。
- 已查看 `test-results/question-crud-mobile.png`：375 像素宽度下右侧编辑/删除可见，标题可点击查看，表格内部横向滚动。
- 本次仅调整前端入口和相关测试、说明；测试使用隔离响应，未执行真实数据库写入或重新联调后端。

## 2026-09-20 只读详情与操作入口调整（历史）

- 点击列表标题打开 `QuestionDetailDialog.vue`，调用现有 `GET /admin/question/{id}` 获取最新详情；不再显示表格操作列，编辑与删除集中在详情底部。Java 后端、数据库未修改。
- 详情完整展示标题、描述、标准 SQL、难度、状态和结果顺序，保留换行和缩进，内容以纯文本渲染。详情 VO 没有时间字段，弹窗不额外拼接列表时间。未知枚举保持原值。
- 详情、编辑与删除确认窗口轮流展示。取消操作重新读取详情，成功写入后返回并更新列表；详情加载失败或返回 null 时提供重试，禁用编辑/删除。关闭时取消请求并忽略迟到响应。
- `npm run build`：通过，用时 28.83 秒；保留已有 Element Plus 主 JS 包约 1.09 MB 的体积提醒。
- `$env:PLAYWRIGHT_CHANNEL='chrome'; npm run test:e2e`：**23 / 23 通过**，用时 43.3 秒。包含原有登录、注册、路由、分页及完整增删改用例，并适配新的详情操作入口。
- 新增覆盖：键盘打开详情、完整最新内容、SQL 空白保留、HTML 按文本显示、未知枚举、只读内容不含表单、失败/null 重试、加载前禁止操作、关闭后切换另一题不被旧响应覆盖、编辑/删除取消返回详情、成功后关闭详情，以及手机长内容滚动和底部按钮可见。
- 已查看 `test-results/question-detail-desktop.png`（1440 像素）与 `test-results/question-detail-mobile.png`（375 像素）。无整页横向溢出，长描述和 SQL 在弹窗内滚动，底部操作保持可见。截图包含隔离的测试题目与边界值，不代表真实题库。
- 浏览器测试通过拦截响应隔离，未用真实账号联调详情及增删改，也未修改真实数据。正常运行调用真实 API，没有加入模拟回退。本次没有重跑代理测试，代理配置未改变。

## 2026-09-20 题目增删改接入（历史）

- 核对最新 Controller、DTO、VO、Service、Mapper 与校验异常处理代码，按实际路径接入新增、详情、更新和删除。Java 后端、权限配置、数据库数据未修改。
- 新增/编辑共用 `QuestionFormDialog.vue`，枚举集中在 `utils/questionOptions.js`；列表继续隐藏标准 SQL，编辑读取最新详情。操作成功自动更新分页，不恢复手动刷新按钮或重复大标题。
- `npm run build`：通过，用时 9.73 秒。主 JS 包约 1.09 MB（gzip 约 362 KB），保留已有 Element Plus 体积提醒。
- 20 项浏览器用例均已获得通过结果。首次全量运行 19 项通过，1 项因测试点击 Element Plus 下拉框内部输入元素被遮挡而失败；调整为实际可点击的外层控件及单选标签后，相关 2 项复测通过（11.6 秒）。复测调整只涉及测试定位与截图，不涉及业务代码。
- 原有 12 项用例覆盖登录、注册、缓存、路由、分页、失败重试及 375/768/1440/1920 像素布局。
- 新增 8 项用例覆盖：必填和标题长度限制、枚举与数字请求值、SQL 空白原样提交、防重复保存、新增进入末页、最新详情回填、完整 PUT 请求、失败保留输入、403 保留会话、详情失败/null 与重试、删除确认/取消、删除失败保留原题、删除空末页回退、删除最后一条后的空状态、详情空响应体 401、手机表单滚动与操作入口。
- 已人工查看桌面列表及桌面/手机编辑弹窗截图。最新弹窗截图位于 `test-results/question-editor-desktop.png`、`test-results/question-editor-mobile.png`，手机管理页截图位于 `test-results/question-crud-mobile.png`。这些图片均使用隔离的测试账号和题目；Playwright 下次运行会重建截图目录。
- 真实服务只读探测：`http://127.0.0.1:5173` 返回 HTTP 200，后端 `http://127.0.0.1:8080/admin/question/page?page=1&pageSize=10` 的未认证请求返回 HTTP 401。
- 本次未使用真实账号执行增删改；浏览器写入测试全部通过拦截响应隔离。仍需用真实账号完成实际接口联调。数据库外键造成的删除失败由现有错误提示展示，前端不绕过约束。权限功能按用户要求留待后续实现。

## 2026-09-20 管理端调整（历史）

- 改为管理员使用的顶部导航与单栏题目表格，展示标题、描述摘要、原始难度/状态/排序敏感值及创建/更新时间；移除学习卡片、日历与练习侧栏。
- 新增共用 SVG 品牌图形与字标，应用于管理端、登录/注册页和浏览器页签。
- `npm run build`：通过，用时 9.26 秒。Element Plus 主包约 1.09 MB（gzip 约 362 KB），仍有超过 500 KB 的体积提醒。
- `$env:PLAYWRIGHT_CHANNEL='chrome'; npm run test:e2e`：**12 / 12 通过**，用时 18.6 秒。测试服务独立使用 `5174`，不影响 `5173` 开发页面。
- 覆盖表单校验、原始密码提交、会话恢复/退出、路由守卫、分页与每页条数、刷新、加载/空/失败/重试及空响应体 401。管理端测试补充了更新时间展示和页面结构检查。
- 自动检查 375、768、1440、1920 像素宽度，无整页横向溢出；人工查看桌面 1440 像素题目页、手机题目页和桌面登录截图，表格在手机内横向滚动，分页正常排列。
- 最新截图：`test-results/questions-1440.png`、`test-results/questions-1920.png`、`test-results/mobile-questions.png`、`test-results/desktop-login.png`。截图中的账号、题目与总数均来自隔离的浏览器测试夹具，不代表真实数据库内容。
- 本次未修改 Java 后端或数据库，未重新执行真实后端联调或代理测试。正常运行仍调用真实 API，失败时不替换为测试数据；前端管理端布局不等同于管理员角色授权。

以下保留历史记录。后端已有后续改动，首次交付时的问题与连接状态不能作为当前结论。

## 2026-09-20 题目页视觉改版（历史，已由管理端布局替代）

- `npm run build`：通过，保留已有 Element Plus 主包体积提醒。
- `npm run test:e2e`（本机 Chrome）：12 / 12 通过，用时 21.9 秒。覆盖原有登录、注册、路由、分页及各类请求状态。
- 已检查 375、768、1440、1920 像素宽度，没有整页横向溢出；侧栏按屏幕宽度收起。查看了手机、1440 与 1920 桌面截图。
- 桌面效果截图在 `test-results/questions-1440.png`、`test-results/questions-1920.png`，手机截图在 `test-results/mobile-questions.png`。这些截图中的题目和总数来自隔离的浏览器测试夹具；正常页面仍请求真实 API。
- 本次只调整前端布局与样式，没有修改后端或数据库，没有重新执行真实后端登录联调。下方为首次交付时的历史验证记录。

## 2026-09-19 首次交付

验证日期：2026-09-19。环境：Windows、Node.js 24.14.0、npm 11.9.0、本机 Chrome 无头模式。

## 已完成

- `npm run build`：通过，生成 `dist/`。Vite 提示主 JS 包约 1.09 MB（gzip 约 362 KB）超过 500 KB 提醒阈值；当前直接注册 Element Plus 以保持第一版配置简单，不影响构建成功，后续可以按需引入组件优化首屏体积。
- `$env:PLAYWRIGHT_CHANNEL='chrome'; npm run test:e2e`：**12 / 12 通过**，最终用时 20.6 秒。
- `npm run test:proxy`：**1 / 1 通过**，确认环境变量后端地址生效，`/api/login`、`/api/register`、分页路径正确去掉 `/api`，查询参数及 Bearer 头保留。
- 已查看 1280 像素桌面登录/题目页和 375 像素手机题目页截图。手机表格在内部横向滚动以容纳字段，整页无横向溢出，分页控件可换行。

## 浏览器测试覆盖

1. 未登录访问列表跳转登录、必填校验、注册入口。
2. 登录密码原样提交、显示/隐藏、提交中防重复、会话存储、刷新恢复、已登录跳过登录页、本地退出；列表不展示标准 SQL。
3. 注册用户名和密码的上下限校验、确认密码一致性、保留密码两端空格、注册成功回填用户名且密码为空。
4. `code: 200` 作为业务失败，显示后端消息，不保存会话。
5. 上下页、每页条数切换回第一页、仅携带支持的参数、刷新和真实响应总数。
6. 加载中、业务错误单处展示、重试和空列表。
7. 空响应体 401 清理会话并跳转，刷新后不发生重定向循环。
8. 登录公开请求不附带后来写入存储的旧 Token；登录 401 保留登录页。
9. 网络失败提示与重新加载入口。
10. 注册 404 提示且停留注册页。
11. 损坏 localStorage 按未登录处理。
12. 375 像素宽度的注册表单和题目页无整页横向溢出。

测试脚本全部位于 `tests/`；模拟 Token、题目和响应仅为测试夹具，既不进入 `src/` 也不进入生产运行逻辑。截图中的题目与数量是明确隔离的测试响应，不代表真实题库。

## 尚未完成的真实联调

检查 `http://localhost:8080/admin/question/page?page=1&pageSize=10` 时连接被拒绝。没有使用真实用户完成注册、登录或数据库分页，也没有用测试 Token 连接真实后端。

源码已确认三个阻塞：注册缺少 `/register` 映射、登录重新 BCrypt encode 后查询哈希、注册正常状态与登录允许状态不一致。详见 README。Java 后端未修改。

请先修复后端、启动服务，按 README 配置地址，再用真实账号依次检查注册、登录、刷新恢复、分页和退出。
