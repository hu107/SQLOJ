<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.oj.mapper.AuthMapper">
    <insert id="register">
        insert into
        user (username, password_hash , role, status, create_time , update_time )
        values (#{username}, #{passwordHash}, #{role}, #{status}, #{createTime}, #{updateTime})
    </insert>
    <select id="login" resultType="com.oj.entity.User">
        select id,
               username,
               password_hash as passwordHash,
               role,
               status,
               create_time as createTime,
               update_time as updateTime
        from user
        where username = #{username}
    </select>
</mapper>